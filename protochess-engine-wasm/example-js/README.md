# Example of a simple JavaScript client

Build and run the example with the following commands:
```
npm install
npm run build
npm start
```

The dev server should be available at http://localhost:8080.
